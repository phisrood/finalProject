<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
				 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CrsesREQ">
	<select id="getScoreViewList" parameterType="map" resultType="scoreView">
		select * from score_view where use_id = #{id} and lec_makeyear = #{year} and lec_makesemester = #{semes}
	</select>
	
	<select id="crsesAllList" parameterType="map" resultType="crsesListView">
		select * from crseslist_view where lec_makeyear = #{year} and lec_makesemester = #{semes}
	</select>
</mapper>
